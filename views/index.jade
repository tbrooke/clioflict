extends layout

block content
  #title-space
    h1= title
  nav
    - var nav = {}; nav[title] = 'active'
    ul
      li(class='#{nav.Query}') Check for Conflict
      li(class='#{nav.Admin}') Admin  
  p Welcome to #{title}